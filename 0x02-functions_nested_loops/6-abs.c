#include "main.h"
/**
 * _abs - computes the absolute value of an integer.
 * @n: The int to be computed
 *
 * Return: absolute int value
 */
int _abs(int n)
{

	if (n < 0)
	{
		n *= -1;
	}
	return (n);
}
